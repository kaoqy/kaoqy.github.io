name: Deploy Hexo Blog

on:
  push:
    branches:
      - main # 修改为你的主分支名称

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Run qexo.sh
  curl -sSL https://kaocdn.us.kg/qexo.sh | bash      
